---
import Layout from './Layout.astro';

interface ProductData {
  title: string;
  slug: string;
  sku?: string;
  skus?: string[];
  price: number;
  badge?: string;
  image: string;
  inStock?: boolean;
  subtitle?: string;
  priceNote?: string;
  paramChips?: string[];
  seoTitle?: string;
  seoDescription?: string;
  features?: string[];
  specs?: {
    color?: string;
    dimensions?: string;
    weight?: string;
    memory?: string;
    battery?: string;
    languages?: string;
    format?: string;
    connection?: string;
    warranty?: string;
    [key: string]: string | undefined;
  };
  boxContents?: string[];
  gallery?: string[];
  colors?: string[];
  heroKeySpecs?: boolean;
}

interface RelatedItem {
  slug: string;
  title: string;
  price: number;
  image: string;
}

interface Props {
  product: ProductData;
  related?: RelatedItem[];
}

const { product, related = [] } = Astro.props;
const price = new Intl.NumberFormat('ru-RU').format(product.price);
const stockLabel = product.inStock === false ? 'Предзаказ' : 'Купить сейчас';
const allSkus = Array.from(new Set([...(Array.isArray(product.skus) ? product.skus : []), ...(product.sku ? [product.sku] : [])]));
const primarySku = allSkus[0] ?? '';
const allSkusCsv = allSkus.join(',');

const gallery = (product.gallery && product.gallery.length ? product.gallery : [product.image]).filter(Boolean);

const specsRows = [
  { label: 'Размеры', value: product.specs?.dimensions },
  { label: 'Вес', value: product.specs?.weight },
  { label: 'Память', value: product.specs?.memory },
  { label: 'Автономность', value: product.specs?.battery },
  { label: 'Языки', value: product.specs?.languages },
  { label: 'Форматы файлов', value: product.specs?.format },
  { label: 'Цвет', value: product.specs?.color },
  { label: 'Подключение', value: product.specs?.connection },
  { label: 'Гарантия', value: product.specs?.warranty }
].filter((row) => row.value);

const keySpecs = [
  { label: 'Память', value: product.specs?.memory },
  { label: 'Автономность', value: product.specs?.battery },
  { label: 'Вес', value: product.specs?.weight },
  { label: 'Подключение', value: product.specs?.connection },
  { label: 'Размеры', value: product.specs?.dimensions },
  { label: 'Языки', value: product.specs?.languages },
].filter((s): s is { label: string; value: string } => !!s.value).slice(0, 4);

function parseSpecDisplay(val: string): { prefix: string; num: string; unit: string } {
  const m = val.match(/^([^\d]*)(\d+(?:[.,]\d+)?)(.*)$/);
  if (m && m[2]) {
    return { prefix: (m[1] ?? '').trim(), num: m[2], unit: (m[3] ?? '').trim() };
  }
  return { prefix: '', num: '', unit: val };
}
const keySpecsParsed = keySpecs.map(s => ({ ...s, parsed: parseSpecDisplay(s.value) }));

const subtitle = product.subtitle ?? 'AI-устройство CRYPTORO';
const priceNote = product.priceNote ?? '';
const paramChips = product.paramChips ?? [];
const colors = product.colors ?? [];

const BOX_ICONS = [
  `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" aria-hidden="true"><rect x="9" y="2" width="6" height="12" rx="3" stroke="currentColor" stroke-width="1.5"/><path d="M5 10a7 7 0 0 0 14 0" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/><line x1="12" y1="17" x2="12" y2="22" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>`,
  `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" aria-hidden="true"><rect x="3" y="6" width="18" height="14" rx="3" stroke="currentColor" stroke-width="1.5"/><path d="M8 6V5a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v1" stroke="currentColor" stroke-width="1.5"/></svg>`,
  `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" aria-hidden="true"><circle cx="12" cy="12" r="8" stroke="currentColor" stroke-width="1.5"/><circle cx="12" cy="12" r="3.5" stroke="currentColor" stroke-width="1.5"/></svg>`,
  `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" aria-hidden="true"><path d="M12 2v5m0 10v5" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/><path d="M9 5h6M9 19h6" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/><rect x="8" y="7" width="8" height="10" rx="2" stroke="currentColor" stroke-width="1.5"/></svg>`,
  `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" aria-hidden="true"><path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" stroke="currentColor" stroke-width="1.5"/><polyline points="14 2 14 8 20 8" stroke="currentColor" stroke-width="1.5"/><line x1="8" y1="13" x2="16" y2="13" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/><line x1="8" y1="17" x2="13" y2="17" stroke="currentColor" stroke-width="1.5" stroke-linecap="round"/></svg>`,
];
---
<Layout title={product.seoTitle ?? `${product.title} | CRYPTORO`} description={product.seoDescription ?? ''} withChrome={true} active="catalog">
  <main class="pdp" data-slug={product.slug}>

    <!-- 1. Hero -->
    <section class="pdp-hero">
      <div class="pdp-container">
        <nav class="pdp-breadcrumbs" aria-label="Хлебные крошки">
          <a href="/">Главная</a>
          <span aria-hidden="true">/</span>
          <a href="/catalog">Каталог</a>
          <span aria-hidden="true">/</span>
          <span>{product.title}</span>
        </nav>

        {product.badge && <span class="pdp-badge">{product.badge}</span>}
        <h1 class="pdp-hero__title">{product.title}</h1>
        <p class="pdp-hero__subtitle">{subtitle}</p>

        <div class="pdp-hero__price-row">
          <span class="pdp-hero__price" data-ms-price={product.slug} data-ms-sku={primarySku} data-ms-skus={allSkusCsv}>{price}<span class="pdp-hero__currency">&nbsp;₽</span></span>
          <span class="pdp-hero__stock" data-ms-stock={product.slug} data-ms-sku={primarySku} data-ms-skus={allSkusCsv}></span>
        </div>
        {priceNote && <p class="pdp-hero__price-note">{priceNote}</p>}

        {product.heroKeySpecs && keySpecsParsed.length > 0 && (
          <div class="pdp-hero__keyspecs">
            {keySpecsParsed.map((spec) => (
              <div class="pdp-hero__keyspec-item">
                <div class="pdp-hero__keyspec-val">
                  {spec.parsed.num ? (
                    <>
                      {spec.parsed.prefix && <span class="pdp-hero__keyspec-prefix">{spec.parsed.prefix}</span>}
                      <span class="pdp-hero__keyspec-num">{spec.parsed.num}</span>
                      {spec.parsed.unit && <span class="pdp-hero__keyspec-unit">{spec.parsed.unit}</span>}
                    </>
                  ) : (
                    <span class="pdp-hero__keyspec-text">{spec.parsed.unit}</span>
                  )}
                </div>
                <span class="pdp-hero__keyspec-label">{spec.label}</span>
              </div>
            ))}
          </div>
        )}

        <div class="pdp-hero__media">
          <div class="pdp-hero__main-img">
            <img id="mainImg" src={gallery[0] ?? product.image} alt={`AI-диктофон ${product.title}`} />
          </div>
        </div>

        {gallery.length > 1 && (
          <div class="pdp-hero__thumbs">
            {gallery.map((image, idx) => (
              <button class={`pdp-thumb ${idx === 0 ? 'active' : ''}`} type="button" data-gallery-thumb data-src={image} aria-label={`Фото ${idx + 1}: ${product.title}`}>
                <img src={image} alt="" />
              </button>
            ))}
          </div>
        )}

        {colors.length > 1 && (
          <div class="pdp-hero__colors">
            <p class="pdp-hero__color-label">Цвет: <span id="selectedColorName">{colors[0]}</span></p>
            <div class="pdp-hero__color-btns" id="colorBtns">
              {colors.map((c, i) => (
                <button class={`color-btn${i === 0 ? ' active' : ''}`} type="button" data-color={c}>{c}</button>
              ))}
            </div>
          </div>
        )}

        <div class="pdp-hero__variants" data-ms-variants={product.slug} data-ms-sku={primarySku} data-ms-skus={allSkusCsv}></div>

        <div class="pdp-hero__actions" id="heroActions">
          <button class="pdp-btn pdp-btn--primary" id="buyBtn" data-ms-buy-btn={product.slug} data-ms-sku={primarySku} data-ms-skus={allSkusCsv} aria-label={`Купить сейчас ${product.title}`}>{stockLabel}</button>
          <button class="pdp-btn pdp-btn--secondary" id="addBtn" aria-label={`Добавить в корзину ${product.title}`}>В корзину</button>
        </div>
        <p class="pdp-hero__trust">Гарантия 1 год &middot; Доставка по РФ &middot; Возврат 14 дней</p>
      </div>
    </section>

    <!-- 2. AI Capability Dark Section -->
    {product.features && product.features.length > 0 && (
      <section class="pdp-ai-dark">
        <div class="pdp-container">
          <p class="pdp-ai-dark__kicker">Plaud Intelligence™</p>
          <h2 class="pdp-ai-dark__headline">{subtitle}</h2>
          <ul class="pdp-ai-dark__list">
            {product.features.map((feat) => (
              <li class="pdp-ai-dark__item">{feat}</li>
            ))}
          </ul>
        </div>
      </section>
    )}

    <!-- 3. Editorial Description -->
    <section class="pdp-editorial">
      <div class="pdp-container">
        <div class="pdp-editorial__copy reveal">
          <h2 class="pdp-section-title">О продукте</h2>
          <div class="pdp-editorial__body">
            <slot />
          </div>
        </div>
      </div>
    </section>

    <!-- 4. Key Specs -->
    {keySpecsParsed.length > 0 && !product.heroKeySpecs && (
      <section class="pdp-key-specs">
        <div class="pdp-container">
          <h2 class="pdp-section-title reveal">Ключевые параметры</h2>
          <div class="pdp-key-specs__grid">
            {keySpecsParsed.map((spec, idx) => (
              <div class="pdp-spec-card reveal" style={`--stagger:${idx}`}>
                <span class="pdp-spec-card__value">
                  {spec.parsed.num ? (
                    <>
                      {spec.parsed.prefix && <span class="pdp-spec-card__prefix">{spec.parsed.prefix}</span>}
                      <span class="pdp-spec-card__num">{spec.parsed.num}</span>
                      {spec.parsed.unit && <span class="pdp-spec-card__unit">{spec.parsed.unit}</span>}
                    </>
                  ) : (
                    <span class="pdp-spec-card__text">{spec.parsed.unit}</span>
                  )}
                </span>
                <span class="pdp-spec-card__label">{spec.label}</span>
              </div>
            ))}
          </div>
        </div>
      </section>
    )}

    <!-- 5+6. Specs + Box — unified details section -->
    {(specsRows.length > 0 || (product.boxContents ?? []).length > 0) && (
      <section class="pdp-details">
        <div class="pdp-container">
          {specsRows.length > 0 && (
            <details class="pdp-accordion reveal" open>
              <summary class="pdp-accordion__trigger">
                <h2 class="pdp-section-title">Характеристики</h2>
                <span class="pdp-accordion__icon" aria-hidden="true">−</span>
              </summary>
              <div class="pdp-accordion__body">
                <table class="pdp-specs-table">
                  <tbody>
                    {specsRows.map((row) => (
                      <tr>
                        <td>{row.label}</td>
                        <td>{row.value}</td>
                      </tr>
                    ))}
                  </tbody>
                </table>
              </div>
            </details>
          )}
          {(product.boxContents ?? []).length > 0 && (
            <details class="pdp-accordion reveal">
              <summary class="pdp-accordion__trigger">
                <h2 class="pdp-section-title">Комплект</h2>
                <span class="pdp-accordion__icon" aria-hidden="true">+</span>
              </summary>
              <div class="pdp-accordion__body">
                <div class="pdp-box-accordion">
                  {(product.boxContents ?? []).map((item, idx) => (
                    <div class="pdp-box-item">
                      <span class="pdp-box-item__icon" set:html={BOX_ICONS[idx] ?? BOX_ICONS[0]} />
                      <span class="pdp-box-item__num">{String(idx + 1).padStart(2, '0')}</span>
                      <span class="pdp-box-item__name">{item}</span>
                    </div>
                  ))}
                </div>
              </div>
            </details>
          )}
        </div>
      </section>
    )}

    <!-- 7. Model Comparison -->
    {!!related.length && (
      <section class="pdp-compare">
        <div class="pdp-container">
          <h2 class="pdp-section-title reveal">Сравните модели</h2>
          <div class="pdp-compare__grid">
            <div class="pdp-compare-card pdp-compare-card--current reveal">
              <span class="pdp-compare-card__badge">Вы смотрите</span>
              <img src={product.image} alt={product.title} />
              <h3>{product.title}</h3>
              <span class="pdp-compare-card__price" data-ms-price={product.slug}>{price}&nbsp;₽</span>
              {keySpecs.length > 0 && (
                <ul class="pdp-compare-card__specs">
                  {keySpecs.slice(0, 3).map((s) => <li><strong>{s.value}</strong> {s.label}</li>)}
                </ul>
              )}
            </div>
            {related.map((item, idx) => (
              <a class="pdp-compare-card pdp-compare-card--alt reveal" style={`--stagger:${idx + 1}`} href={`/products/${item.slug}`}>
                <img src={item.image} alt={item.title} />
                <h3>{item.title}</h3>
                <span class="pdp-compare-card__price" data-ms-price={item.slug}>{new Intl.NumberFormat('ru-RU').format(item.price)}&nbsp;₽</span>
                <span class="pdp-compare-card__cta">Подробнее →</span>
              </a>
            ))}
          </div>
        </div>
      </section>
    )}

  </main>

  <!-- Sticky Mobile Buy Bar -->
  <div class="pdp-sticky-bar" id="stickyBar">
    <div class="pdp-sticky-bar__inner">
      <div class="pdp-sticky-bar__info">
        <span class="pdp-sticky-bar__name">{product.title}</span>
        <span class="pdp-sticky-bar__price" data-ms-price={product.slug}>{price}&nbsp;₽</span>
      </div>
      <button class="pdp-btn pdp-btn--primary pdp-btn--sm" id="stickyBuyBtn">Купить</button>
    </div>
  </div>

  <!-- Lightbox -->
  <div class="lightbox" id="productLightbox" aria-hidden="true">
    <button type="button" class="lightbox-close" id="lightboxClose" aria-label="Закрыть просмотр">&times;</button>
    <img id="lightboxImg" src={gallery[0] ?? product.image} alt={`Увеличенное фото ${product.title}`} />
  </div>

  <!-- Cart Toast -->
  <div class="cart-toast" id="cartToast" role="status" aria-live="polite">Товар добавлен в корзину</div>

  <!-- Cart & Color JS (preserved) -->
  <script define:vars={{ baseProduct: { id: product.slug, name: product.title, price: product.price, image: product.image }, hasColors: colors.length > 1 }} is:inline>
    function showToast() {
      const toast = document.getElementById('cartToast');
      if (!toast) return;
      toast.classList.add('show');
      window.setTimeout(() => toast.classList.remove('show'), 3000);
    }

    function getCartCountSafe() {
      if (window.cryptoroCart && typeof window.cryptoroCart.readItems === 'function') {
        const items = window.cryptoroCart.readItems();
        return items.reduce((sum, item) => sum + Math.max(1, Number(item?.qty) || 1), 0);
      }
      return 0;
    }

    function getSelectedColor() {
      if (!hasColors) return '';
      const btn = document.querySelector('#colorBtns .color-btn.active');
      return btn ? (btn.dataset.color || '') : '';
    }

    function buildCartItem() {
      const color = getSelectedColor();
      return {
        id: color ? baseProduct.id + '::' + color : baseProduct.id,
        name: baseProduct.name,
        price: baseProduct.price,
        image: baseProduct.image,
        qty: 1,
        color: color || undefined
      };
    }

    function addToCartItem() {
      if (window.cryptoroCart && typeof window.cryptoroCart.add === 'function') {
        window.cryptoroCart.add(buildCartItem());
      }
      const nextCount = getCartCountSafe();
      const c = document.querySelector('#cartCount, .cart-n');
      if (c) {
        c.textContent = String(nextCount);
        c.style.display = nextCount > 0 ? 'flex' : 'none';
      }
      window.dispatchEvent(new CustomEvent('cart:updated', { detail: { count: nextCount } }));
      showToast();
      const btn = document.getElementById('addBtn');
      if (!btn) return;
      const original = btn.textContent;
      btn.textContent = '\u2713 Добавлено';
      setTimeout(() => { btn.textContent = original ?? 'В корзину'; }, 2000);
    }

    function buyNowItem() {
      addToCartItem();
      window.location.href = '/cart';
    }

    document.addEventListener('DOMContentLoaded', () => {
      const addBtn = document.getElementById('addBtn');
      const buyBtn = document.getElementById('buyBtn');
      if (addBtn) addBtn.addEventListener('click', addToCartItem);
      if (buyBtn) buyBtn.addEventListener('click', buyNowItem);

      // Sticky buy bar
      const stickyBuyBtn = document.getElementById('stickyBuyBtn');
      if (stickyBuyBtn) stickyBuyBtn.addEventListener('click', buyNowItem);

      const heroActions = document.getElementById('heroActions');
      const stickyBar = document.getElementById('stickyBar');
      if (heroActions && stickyBar) {
        const obs = new IntersectionObserver(([entry]) => {
          stickyBar.classList.toggle('pdp-sticky-bar--visible', !entry.isIntersecting);
        }, { threshold: 0 });
        obs.observe(heroActions);

        // Hide sticky bar when footer enters viewport
        const footer = document.querySelector('footer');
        if (footer) {
          const footerObs = new IntersectionObserver(([entry]) => {
            if (entry.isIntersecting) stickyBar.classList.remove('pdp-sticky-bar--visible');
          }, { threshold: 0 });
          footerObs.observe(footer);
        }
      }

      // Stock badge fallback: show "Нет данных" if API doesn't respond
      const stockBadge = document.querySelector('[data-ms-stock]');
      if (stockBadge instanceof HTMLElement) {
        window.setTimeout(() => {
          if (stockBadge.style.display === 'none' || !stockBadge.textContent.trim()) {
            stockBadge.textContent = 'Нет данных';
            stockBadge.classList.add('ms-stock-badge');
            stockBadge.style.display = 'inline-flex';
          }
        }, 4500);
      }

      // Accordion icon
      document.querySelectorAll('.pdp-accordion').forEach((el) => {
        el.addEventListener('toggle', () => {
          const icon = el.querySelector('.pdp-accordion__icon');
          if (icon) icon.textContent = el.open ? '\u2212' : '+';
        });
      });

      // Color picker
      const colorBtns = document.getElementById('colorBtns');
      if (colorBtns) {
        colorBtns.addEventListener('click', (event) => {
          const btn = event.target.closest('.color-btn');
          if (!btn) return;
          colorBtns.querySelectorAll('.color-btn').forEach((b) => b.classList.remove('active'));
          btn.classList.add('active');
          const label = document.getElementById('selectedColorName');
          if (label) label.textContent = btn.dataset.color || '';
        });
      }
    });
  </script>

  <!-- Gallery & Lightbox JS -->
  <script>
    (function () {
      const mainImg = document.getElementById('mainImg');
      const lightbox = document.getElementById('productLightbox');
      const lightboxImg = document.getElementById('lightboxImg');
      const closeBtn = document.getElementById('lightboxClose');
      if (!mainImg || !lightbox || !lightboxImg || !closeBtn) return;

      function openLightbox(src) {
        if (src) lightboxImg.src = src;
        lightbox.style.display = 'flex';
        requestAnimationFrame(() => lightbox.classList.add('visible'));
        lightbox.setAttribute('aria-hidden', 'false');
      }

      function closeLightbox() {
        lightbox.classList.remove('visible');
        lightbox.setAttribute('aria-hidden', 'true');
        window.setTimeout(() => {
          lightbox.style.display = 'none';
        }, 200);
      }

      document.querySelectorAll('[data-gallery-thumb]').forEach((thumb) => {
        thumb.addEventListener('click', () => {
          const src = thumb.getAttribute('data-src');
          if (!src) return;
          mainImg.src = src;
          document.querySelectorAll('[data-gallery-thumb]').forEach((el) => el.classList.remove('active'));
          thumb.classList.add('active');
        });
      });

      mainImg.addEventListener('click', () => openLightbox(mainImg.src));
      closeBtn.addEventListener('click', closeLightbox);
      lightbox.addEventListener('click', (event) => {
        if (event.target === lightbox) closeLightbox();
      });
      document.addEventListener('keydown', (event) => {
        if (event.key === 'Escape') closeLightbox();
      });
    })();
  </script>

  <!-- JSON-LD: Product -->
  <script type="application/ld+json" set:html={JSON.stringify({
    '@context': 'https://schema.org',
    '@type': 'Product',
    name: product.title,
    description: product.seoDescription,
    image: product.image,
    brand: { '@type': 'Brand', name: 'CRYPTORO' },
    offers: {
      '@type': 'Offer',
      priceCurrency: 'RUB',
      price: product.price,
      availability: product.inStock === false ? 'https://schema.org/PreOrder' : 'https://schema.org/InStock'
    }
  })} />

  <!-- JSON-LD: Breadcrumbs -->
  <script type="application/ld+json" set:html={JSON.stringify({
    '@context': 'https://schema.org',
    '@type': 'BreadcrumbList',
    itemListElement: [
      { '@type': 'ListItem', position: 1, name: 'Главная', item: 'https://cryptoro.ru/' },
      { '@type': 'ListItem', position: 2, name: 'Каталог', item: 'https://cryptoro.ru/catalog' },
      { '@type': 'ListItem', position: 3, name: product.title, item: `https://cryptoro.ru/products/${product.slug}` }
    ]
  })} />

  <style is:global>
    /* ===== PDP Design Tokens ===== */
    :root{
      --pdp-bg:#f5f5f7;
      --pdp-surface:#ffffff;
      --pdp-text:#111;
      --pdp-muted:#86868b;
      --pdp-accent:#e88635;
      --pdp-radius:24px;
      --pdp-shadow:none;
    }

    /* ===== Base ===== */
    .pdp{background:var(--pdp-surface);color:var(--pdp-text);font-family:'Inter',sans-serif}
    .pdp-container{max-width:980px;margin:0 auto;padding:0 24px}

    /* ===== Section Title ===== */
    .pdp-section-title{font-family:'Unbounded',sans-serif;font-size:clamp(26px,3.2vw,34px);font-weight:700;line-height:1.2;letter-spacing:-.02em;margin:0 0 28px;color:var(--pdp-text)}

    /* ===== 1. Hero ===== */
    .pdp-hero{background:linear-gradient(180deg,#fafafa 0%,var(--pdp-surface) 100%)!important;padding:92px 0 32px;text-align:center}
    .pdp-breadcrumbs{display:flex;align-items:center;justify-content:center;gap:8px;margin-bottom:20px;font-size:13px;color:var(--pdp-muted)}
    .pdp-breadcrumbs a{text-decoration:none;color:#6e6e73;transition:color .15s}
    .pdp-breadcrumbs a:hover{color:var(--pdp-text)}
    .pdp-badge{display:inline-block;background:var(--pdp-accent);color:#fff;border-radius:20px;padding:5px 14px;font-size:10px;font-weight:700;letter-spacing:.1em;text-transform:uppercase;margin-bottom:14px}
    .pdp-hero__title{font-family:'Unbounded',sans-serif;font-size:clamp(52px,7vw,84px);font-weight:900;line-height:1.05;letter-spacing:-.03em;margin:0 0 8px;overflow:hidden}
    .pdp-hero__subtitle{font-size:clamp(17px,1.8vw,20px);line-height:1.45;color:var(--pdp-muted);margin:0 auto 10px;max-width:34ch}
    .pdp-hero__price-row{display:flex;align-items:baseline;justify-content:center;gap:12px;margin-bottom:4px}
    .pdp-hero__price{font-family:'Unbounded',sans-serif;font-size:clamp(32px,4vw,44px);font-weight:600;line-height:1.1;letter-spacing:-.02em;color:#222}
    .pdp-hero__currency{font-size:.65em;font-weight:400;color:var(--pdp-muted);letter-spacing:0}
    .pdp-hero__stock{display:none;align-items:center;padding:3px 9px;font-size:11px;font-weight:500;color:#6e6e73;background:rgba(0,0,0,.04);border:1px solid rgba(0,0,0,.1);border-radius:999px;letter-spacing:.01em}
    .pdp-hero__price-note{margin:0 0 6px;color:var(--pdp-muted);font-size:14px}

    /* Override global MS badge colors to match premium PDP palette */
    .pdp-hero__stock.ms-in-stock{color:#15803d;background:rgba(21,128,61,.06);border-color:rgba(21,128,61,.16)}
    .pdp-hero__stock.ms-low-stock{color:#92400e;background:rgba(146,64,14,.06);border-color:rgba(146,64,14,.16)}
    .pdp-hero__stock.ms-preorder{color:#6b7280;background:rgba(0,0,0,.05);border-color:rgba(0,0,0,.12)}

    /* ===== Mini Hero Key Specs (heroKeySpecs mode) ===== */
    .pdp-hero__keyspecs{display:grid;grid-template-columns:repeat(4,1fr);max-width:500px;margin:20px auto 0;border:1px solid rgba(0,0,0,.08);border-radius:16px;overflow:hidden}
    .pdp-hero__keyspec-item{padding:13px 10px;text-align:center;border-right:1px solid rgba(0,0,0,.08)}
    .pdp-hero__keyspec-item:last-child{border-right:none}
    .pdp-hero__keyspec-val{display:flex;align-items:baseline;justify-content:center;gap:.06em;white-space:nowrap;margin-bottom:4px}
    .pdp-hero__keyspec-num{font-family:'Unbounded',sans-serif;font-size:clamp(13px,1.5vw,17px);font-weight:700;letter-spacing:-.02em;color:var(--pdp-text);line-height:1}
    .pdp-hero__keyspec-unit{font-size:.7em;font-weight:600;color:var(--pdp-muted);margin-left:.05em}
    .pdp-hero__keyspec-prefix{font-size:.58em;font-weight:400;color:var(--pdp-muted)}
    .pdp-hero__keyspec-text{font-family:'Unbounded',sans-serif;font-size:clamp(9px,1.1vw,12px);font-weight:600;letter-spacing:-.01em;color:var(--pdp-text);line-height:1.2}
    .pdp-hero__keyspec-label{display:block;font-size:9px;color:var(--pdp-muted);font-weight:500;text-transform:uppercase;letter-spacing:.07em;line-height:1.3}

    /* Hero Media — larger image */
    .pdp-hero__media{margin:24px auto 0;max-width:820px}
    .pdp-hero__main-img{aspect-ratio:4/3;display:flex;align-items:center;justify-content:center;overflow:hidden}
    .pdp-hero__main-img img{width:92%;max-width:720px;object-fit:contain;cursor:zoom-in;transition:transform .5s cubic-bezier(.25,.1,.25,1)}
    .pdp-hero__main-img:hover img{transform:scale(1.05)}

    /* Thumbs */
    .pdp-hero__thumbs{display:flex;gap:10px;justify-content:center;margin-top:16px;flex-wrap:wrap}
    .pdp-thumb{border:1.5px solid transparent;border-radius:12px;background:var(--pdp-bg);padding:6px;cursor:pointer;transition:all .2s ease;display:flex;align-items:center;justify-content:center}
    .pdp-thumb.active{border-color:var(--pdp-text);background:var(--pdp-surface)}
    .pdp-thumb img{width:52px;height:52px;object-fit:contain;display:block}
    .pdp-thumb:focus-visible{outline:none;box-shadow:0 0 0 3px rgba(232,134,53,.22)}

    /* Colors */
    .pdp-hero__colors{margin-top:20px}
    .pdp-hero__color-label{font-size:14px;font-weight:600;color:var(--pdp-text);margin:0 0 10px}
    .pdp-hero__color-label span{font-weight:400;color:var(--pdp-muted)}
    .pdp-hero__color-btns{display:flex;flex-wrap:wrap;gap:8px;justify-content:center}
    .color-btn{height:36px;padding:0 14px;border-radius:20px;border:1.5px solid rgba(0,0,0,.12);background:#fff;color:#374151;font-size:13px;font-weight:500;cursor:pointer;transition:all .18s ease;line-height:1}
    .color-btn:hover:not([disabled]){border-color:rgba(0,0,0,.35);color:#111}
    .color-btn.active{border-color:#111;background:#111;color:#fff}
    .color-btn[disabled]{opacity:.38;cursor:not-allowed;text-decoration:line-through}
    .color-btn.is-preorder{opacity:.7;border-style:dashed}
    .color-btn.is-low{border-color:#f59e0b}
    .color-stock{font-size:10px;margin-left:5px;opacity:.65;font-weight:400}
    .color-btn.active .color-stock{opacity:.85;color:#fff}

    /* Variants (populated by Moysklad) */
    .pdp-hero__variants{display:none!important}

    /* Actions */
    .pdp-hero__actions{display:flex;gap:12px;justify-content:center;margin-top:28px;max-width:400px;margin-left:auto;margin-right:auto}

    /* ===== Button System ===== */
    .pdp-btn{display:inline-flex;align-items:center;justify-content:center;border:none;border-radius:14px;padding:14px 32px;min-height:52px;font:600 15px/1.2 'Inter',sans-serif;cursor:pointer;transition:all .2s ease-out;text-align:center;white-space:nowrap;flex:1}
    .pdp-btn--primary{background:#111;color:#fff;box-shadow:0 2px 8px rgba(0,0,0,.1)}
    .pdp-btn--primary:hover{transform:translateY(-2px);box-shadow:0 8px 24px rgba(0,0,0,.18)}
    .pdp-btn--primary:active{transform:translateY(0) scale(.995);box-shadow:0 2px 8px rgba(0,0,0,.1)}
    .pdp-btn--primary:focus-visible{outline:none;box-shadow:0 0 0 3px rgba(17,17,17,.18),0 8px 20px rgba(0,0,0,.14)}
    .pdp-btn--secondary{background:var(--pdp-surface);color:#111;border:1.5px solid rgba(0,0,0,.12)}
    .pdp-btn--secondary:hover{transform:translateY(-1px);box-shadow:0 4px 16px rgba(0,0,0,.06);border-color:rgba(0,0,0,.3)}
    .pdp-btn--secondary:active{transform:translateY(0) scale(.995)}
    .pdp-btn--secondary:focus-visible{outline:none;box-shadow:0 0 0 3px rgba(232,134,53,.22)}
    .pdp-btn--sm{padding:10px 20px;min-height:42px;font-size:14px;flex:0 0 auto}
    .pdp-btn.is-disabled,[data-ms-buy-btn].is-disabled{opacity:.55!important;cursor:not-allowed!important;box-shadow:none!important;transform:none!important}

    /* ===== 2. Key Benefits — typographic, no cards ===== */
    .pdp-benefits{background:var(--pdp-surface);padding:40px 0 0;border-top:1px solid rgba(0,0,0,.06)}
    .pdp-benefits__grid{display:flex;justify-content:center;gap:48px;max-width:700px;margin:0 auto}
    .pdp-benefit{background:none;border:none;border-radius:0;padding:0;text-align:center;box-shadow:none;transition:none;white-space:nowrap}
    .pdp-benefit__text{font-size:15px;font-weight:600;color:var(--pdp-text);line-height:1.4;letter-spacing:-.01em}

    /* ===== 3. Editorial ===== */
    .pdp-editorial{background:var(--pdp-surface);padding:88px 0 72px}
    .pdp-editorial__grid{display:grid;grid-template-columns:1fr 1fr;gap:48px;align-items:center}
    .pdp-editorial__lead{font-size:20px;line-height:1.65;color:#4b5563;margin:0 0 16px}
    .pdp-editorial__body{font-size:18px;color:#4b5563;line-height:1.72}
    .pdp-editorial__body p{margin:0 0 20px}
    .pdp-editorial__body p:last-child{margin-bottom:0}
    .pdp-editorial__image{border-radius:20px;overflow:hidden}
    .pdp-editorial__image img{width:100%;height:100%;object-fit:cover;display:block;border-radius:20px}

    /* ===== 4. Key Specs — typographic 2x2 grid, no cards ===== */
    .pdp-key-specs{background:var(--pdp-bg)!important;padding:112px 0 96px}
    .pdp-key-specs__grid{display:grid;grid-template-columns:repeat(2,1fr);gap:0;max-width:640px;margin:0 auto}
    .pdp-spec-card{background:none;border:none;border-radius:0;padding:36px 24px;text-align:center;box-shadow:none;transition:none;position:relative}
    /* subtle grid lines */
    .pdp-spec-card:nth-child(odd){border-right:1px solid rgba(0,0,0,.08)}
    .pdp-spec-card:nth-child(-n+2){border-bottom:1px solid rgba(0,0,0,.08)}
    .pdp-spec-card__value{display:flex;align-items:baseline;justify-content:center;gap:.1em;flex-wrap:wrap;margin-bottom:8px;white-space:nowrap}
    .pdp-spec-card__num{font-family:'Unbounded',sans-serif;font-size:clamp(30px,4vw,42px);font-weight:800;line-height:1.08;letter-spacing:-.025em;color:var(--pdp-text)}
    .pdp-spec-card__unit{font-size:.7em;font-weight:600;letter-spacing:-.01em;color:var(--pdp-muted);margin-left:.08em}
    .pdp-spec-card__prefix{font-size:.48em;font-weight:500;color:var(--pdp-muted);margin-right:.06em;align-self:center}
    .pdp-spec-card__text{font-family:'Unbounded',sans-serif;font-size:clamp(15px,1.9vw,20px);font-weight:600;letter-spacing:-.015em;color:var(--pdp-text);line-height:1.2}
    .pdp-spec-card__label{display:block;font-size:12px;color:var(--pdp-muted);font-weight:500;text-transform:uppercase;letter-spacing:.08em}

    /* ===== 5+6. Details — unified specs + box section ===== */
    .pdp-details{background:var(--pdp-surface);padding:64px 0}
    /* Each accordion gets top border; last also gets bottom */
    .pdp-accordion{border:none;border-top:1px solid rgba(0,0,0,.08);border-radius:0;overflow:hidden}
    .pdp-accordion:last-child{border-bottom:1px solid rgba(0,0,0,.08)}
    /* Legacy: single accordion pages still look correct */
    .pdp-full-specs .pdp-accordion{border-bottom:1px solid rgba(0,0,0,.08)}
    .pdp-accordion__trigger{display:flex;align-items:center;justify-content:space-between;padding:24px 0;cursor:pointer;list-style:none;user-select:none}
    .pdp-accordion__trigger::-webkit-details-marker{display:none}
    .pdp-accordion__trigger .pdp-section-title{margin:0}
    .pdp-accordion__icon{font-size:24px;font-weight:300;color:var(--pdp-muted);transition:transform .2s ease;flex-shrink:0;width:32px;height:32px;display:flex;align-items:center;justify-content:center;border-radius:50%;background:none}
    .pdp-accordion__body{padding:0 0 24px}
    .pdp-specs-table{width:100%;border-collapse:collapse;border-spacing:0;overflow:hidden;border-radius:0;border:none}
    .pdp-specs-table td{border-bottom:1px solid rgba(0,0,0,.06);padding:16px 0;overflow-wrap:anywhere;background:none!important}
    .pdp-specs-table tr:last-child td{border-bottom:none}
    .pdp-specs-table td:first-child{color:var(--pdp-muted);width:40%;font-size:13px;font-weight:600;text-transform:uppercase;letter-spacing:.04em}
    .pdp-specs-table td:last-child{color:var(--pdp-text);font-size:15px;font-weight:500}
    /* Box contents inside accordion */
    .pdp-box-accordion{display:grid;grid-template-columns:repeat(auto-fit,minmax(140px,1fr));gap:0;padding-bottom:8px}
    .pdp-box-item{background:none;border:none;border-radius:0;padding:16px;box-shadow:none;transition:none;text-align:center}
    .pdp-box-item__num{display:block;font-family:'Unbounded',sans-serif;font-size:28px;font-weight:800;color:rgba(0,0,0,.06);margin-bottom:6px;line-height:1}
    .pdp-box-item__name{display:block;font-size:14px;font-weight:600;color:var(--pdp-text);line-height:1.4}

    /* ===== 7. Model Comparison — lighter ===== */
    .pdp-compare{background:var(--pdp-bg)!important;padding:96px 0}
    .pdp-compare__grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(220px,1fr));gap:16px}
    .pdp-compare-card{display:flex;flex-direction:column;align-items:center;text-align:center;background:var(--pdp-surface);border:1px solid rgba(0,0,0,.07);border-radius:20px;padding:32px 20px;text-decoration:none;color:inherit;transition:transform .3s ease,box-shadow .3s ease;box-shadow:0 2px 8px rgba(0,0,0,.04)}
    .pdp-compare-card--current{transform:scale(1.03);border-color:rgba(0,0,0,.1);box-shadow:0 8px 32px rgba(0,0,0,.1)}
    .pdp-compare-card__badge{display:inline-block;background:var(--pdp-text);color:#fff;border-radius:20px;padding:4px 12px;font-size:10px;font-weight:700;letter-spacing:.06em;text-transform:uppercase;margin-bottom:16px}
    .pdp-compare-card img{width:100%;max-width:180px;height:160px;object-fit:contain;margin-bottom:16px}
    .pdp-compare-card h3{font-family:'Unbounded',sans-serif;font-size:17px;font-weight:700;margin:0 0 6px;letter-spacing:-.01em}
    .pdp-compare-card__price{font-size:19px;font-weight:700;margin-bottom:12px;display:block}
    .pdp-compare-card__specs{list-style:none;padding:0;margin:0;font-size:13px;color:var(--pdp-muted);line-height:1.8}
    .pdp-compare-card__specs strong{color:var(--pdp-text);font-weight:600}
    .pdp-compare-card__cta{display:inline-flex;align-items:center;justify-content:center;margin-top:auto;padding:10px 24px;background:#111;color:#fff;border-radius:12px;font-size:14px;font-weight:600;transition:all .2s ease}

    /* ===== 8. Trust Bar ===== */
    .pdp-trust{background:var(--pdp-surface);padding:32px 0 48px;border-top:1px solid rgba(0,0,0,.06)}
    .pdp-trust__row{display:flex;align-items:center;justify-content:center;gap:16px;font-size:13px;color:var(--pdp-muted);font-weight:500}
    .pdp-trust__sep{color:rgba(0,0,0,.15)}

    /* ===== AI Capability Dark Section ===== */
    .pdp-ai-dark{background:#111!important;padding:96px 0}
    .pdp-ai-dark .pdp-container{max-width:720px;text-align:center}
    .pdp-ai-dark__kicker{font-size:11px;font-weight:600;letter-spacing:.14em;text-transform:uppercase;color:rgba(255,255,255,.35);margin:0 0 20px;font-family:'Inter',sans-serif}
    .pdp-ai-dark__headline{font-family:'Unbounded',sans-serif;font-size:clamp(28px,3.8vw,48px);font-weight:800;line-height:1.12;letter-spacing:-.035em;color:#fff;margin:0 0 48px}
    .pdp-ai-dark__list{list-style:none;padding:0;margin:0;border-top:1px solid rgba(255,255,255,.1)}
    .pdp-ai-dark__item{padding:22px 0;border-bottom:1px solid rgba(255,255,255,.1);font-size:clamp(16px,1.8vw,19px);font-weight:500;color:rgba(255,255,255,.72);line-height:1.5;letter-spacing:-.01em}

    /* ===== Sticky Buy Bar ===== */
    .pdp-sticky-bar{position:fixed;bottom:0;left:0;right:0;z-index:90;background:rgba(255,255,255,.95);backdrop-filter:blur(20px);border-top:1px solid rgba(0,0,0,.08);transform:translateY(100%);transition:transform .3s ease;pointer-events:none;padding-bottom:env(safe-area-inset-bottom,0px)}
    .pdp-sticky-bar--visible{transform:translateY(0);pointer-events:auto}
    .pdp-sticky-bar__inner{max-width:980px;margin:0 auto;padding:12px 24px;display:flex;align-items:center;justify-content:space-between;gap:16px}
    /* Bottom padding so last section isn't hidden under sticky bar */
    .pdp { padding-bottom: calc(64px + env(safe-area-inset-bottom,0px)) }
    .pdp-sticky-bar__info{display:flex;flex-direction:column;gap:2px;min-width:0}
    .pdp-sticky-bar__name{font-size:14px;font-weight:600;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
    .pdp-sticky-bar__price{font-size:16px;font-weight:700}

    /* ===== Lightbox ===== */
    .lightbox{position:fixed;top:0;left:0;width:100%;height:100%;background:rgba(0,0,0,.85);display:none;align-items:center;justify-content:center;z-index:1000;padding:20px;opacity:0;transition:opacity .2s ease}
    .lightbox.visible{opacity:1}
    .lightbox img{max-width:90vw;max-height:90vh;object-fit:contain;border-radius:16px;background:#fff;padding:10px}
    .lightbox-close{position:absolute;top:18px;right:18px;width:42px;height:42px;border-radius:999px;border:1px solid rgba(255,255,255,.4);background:rgba(0,0,0,.35);color:#fff;font-size:30px;line-height:1;cursor:pointer}

    /* ===== Cart Toast ===== */
    .cart-toast{position:fixed;right:18px;bottom:22px;background:#111;color:#fff;padding:12px 16px;border-radius:12px;font-size:14px;font-weight:600;box-shadow:0 10px 24px rgba(0,0,0,.22);opacity:0;transform:translateY(10px);pointer-events:none;transition:all .24s ease;z-index:310}
    .cart-toast.show{opacity:1;transform:translateY(0)}

    /* ===== Stagger Animations ===== */
    .pdp-benefit[style*="--stagger"]{transition-delay:calc(var(--stagger) * 60ms)}
    .pdp-spec-card[style*="--stagger"]{transition-delay:calc(var(--stagger) * 60ms)}
    .pdp-box-item[style*="--stagger"]{transition-delay:calc(var(--stagger) * 60ms)}
    .pdp-compare-card[style*="--stagger"]{transition-delay:calc(var(--stagger) * 80ms)}

    /* ===== Hover (desktop only) ===== */
    @media(hover:hover){
      .pdp-compare-card:not(.pdp-compare-card--current):hover{transform:translateY(-3px);box-shadow:0 8px 24px rgba(0,0,0,.08)}
    }

    /* ===== Responsive: Tablet ===== */
    @media(max-width:980px){
      .pdp-editorial__grid{grid-template-columns:1fr;gap:32px}
      .pdp-editorial__image{max-height:400px}
      .pdp-compare__grid{grid-template-columns:repeat(2,1fr)}
    }

    /* ===== Responsive: Mobile ===== */
    @media(max-width:640px){
      .pdp-container{padding:0 18px}
      .pdp-hero{padding:84px 0 28px}
      .pdp-hero__keyspecs{max-width:100%;border-radius:12px}
      .pdp-hero__keyspec-item{padding:10px 6px}
      .pdp-hero__keyspec-num{font-size:12px}
      .pdp-hero__keyspec-text{font-size:9px}
      .pdp-hero__keyspec-label{font-size:8px}
      .pdp-hero__title{font-size:clamp(38px,10vw,52px)}
      .pdp-hero__price{font-size:clamp(28px,7vw,36px)}
      .pdp-hero__media{margin-top:20px}
      .pdp-hero__main-img img{width:92%;max-width:440px}
      .pdp-hero__thumbs{flex-wrap:nowrap;overflow-x:auto;justify-content:flex-start;padding:0 18px 4px}
      .pdp-hero__thumbs::-webkit-scrollbar{height:6px}
      .pdp-hero__thumbs::-webkit-scrollbar-thumb{background:rgba(17,24,39,.16);border-radius:999px}
      .pdp-thumb{flex:0 0 auto}
      .pdp-hero__actions{flex-direction:column;max-width:100%;padding:0 18px}
      .pdp-btn{width:100%;flex:initial}
      .pdp-section-title{font-size:clamp(22px,5.5vw,26px);margin-bottom:20px}
      .pdp-ai-dark{padding:64px 0}
      .pdp-ai-dark__headline{font-size:clamp(24px,7vw,34px);margin-bottom:32px}
      .pdp-ai-dark__item{font-size:15px;padding:18px 0}
      .pdp-benefits__grid{gap:24px}
      .pdp-key-specs__grid{max-width:100%}
      .pdp-editorial{padding:56px 0 48px}
      .pdp-editorial__lead{font-size:17px}
      .pdp-editorial__body{font-size:16px}
      .pdp-key-specs{padding:72px 0 56px}
      .pdp-spec-card__num{font-size:clamp(24px,7vw,32px)}
      .pdp-spec-card__text{font-size:clamp(14px,4vw,18px)}
      .pdp-full-specs{padding:40px 0}
      .pdp-box{padding:40px 0}
      .pdp-compare{padding:56px 0}
      .pdp-compare__grid{grid-template-columns:1fr}
      .pdp-compare-card--current{transform:none}
      .pdp-accordion__trigger{padding:20px 0}
      .pdp-accordion__body{padding:0 0 20px}
      .pdp-trust__row{flex-direction:column;gap:8px}
      .pdp-trust__sep{display:none}
    }

    /* ===== Responsive: Small phones ===== */
    @media(max-width:390px){
      .pdp-container{padding:0 16px}
      .pdp-hero{padding:76px 0 28px}
      .pdp-hero__keyspecs{grid-template-columns:repeat(2,1fr)}
      .pdp-hero__keyspec-item:nth-child(odd){border-right:1px solid rgba(0,0,0,.08)}
      .pdp-hero__keyspec-item:nth-child(even){border-right:none}
      .pdp-hero__keyspec-item:nth-child(-n+2){border-bottom:1px solid rgba(0,0,0,.08)}
      .pdp-hero__main-img img{width:94%;max-width:320px}
      .pdp-thumb img{width:44px;height:44px}
      .pdp-thumb{padding:5px}
      .pdp-benefits__grid{flex-direction:column;gap:16px}
      .pdp-spec-card{padding:24px 16px}
      .pdp-spec-card:nth-child(odd){border-right:none}
      .pdp-key-specs__grid{grid-template-columns:1fr}
      .pdp-spec-card:nth-child(-n+2){border-bottom:none}
      .pdp-spec-card{border-bottom:1px solid rgba(0,0,0,.08)}
      .pdp-spec-card:last-child{border-bottom:none}
      .pdp-box__grid{grid-template-columns:repeat(2,1fr)}
    }

    /* ── PDP Fixes ── */

    /* A. Hero: remove fixed aspect-ratio — image sets its own height */
    .pdp-hero__media { margin: 8px auto 0 !important }
    .pdp-hero__main-img { aspect-ratio: auto !important }
    .pdp-hero__main-img img { object-position: center center }

    /* B. CTA: tighter gap to photo, wider buttons */
    .pdp-hero__actions { max-width: 500px !important; margin-top: 12px !important }

    /* C. Mini trust line in hero */
    .pdp-hero__trust {
      margin: 14px auto 0;
      font-size: 12px;
      color: var(--pdp-muted) !important;
      font-weight: 400;
      letter-spacing: .01em;
    }

    /* D. Thumbnails */
    .pdp-thumb img { width: 64px !important; height: 64px !important }

    /* E. Fix Layout.astro global colour overrides */
    .pdp-hero__price { color: #1d1d1f !important }
    .pdp-hero__currency { color: var(--pdp-muted) !important }
    .pdp-spec-card__num { color: var(--pdp-text) !important }
    .pdp-compare-card__price { color: var(--pdp-text) !important }
    .pdp-sticky-bar__price { color: var(--pdp-text) !important }
    /* Dark section: override global h2/p/li colour rules */
    .pdp-ai-dark__kicker { color: rgba(255,255,255,.35) !important }
    .pdp-ai-dark__headline { color: #fff !important }
    .pdp-ai-dark__item { color: rgba(255,255,255,.72) !important }

    /* F. Spec card unit: allow multi-word units to wrap, cap width */
    .pdp-spec-card__value { flex-wrap: wrap !important; gap: .06em .16em !important }
    .pdp-spec-card__unit { white-space: normal !important; max-width: 18ch; line-height: 1.3; text-align: center }

    /* ── 1. Key specs: whitespace-only separation, no divider lines ── */
    .pdp-key-specs { padding: 80px 0 64px !important }
    .pdp-key-specs__grid { max-width: 680px !important; gap: 0 !important }
    .pdp-spec-card {
      padding: 40px 32px !important;
      border: none !important;
      display: flex !important;
      flex-direction: column !important;
      justify-content: center !important;
    }
    /* Remove ALL divider lines */
    .pdp-spec-card:nth-child(odd) { border-right: none !important }
    .pdp-spec-card:nth-child(-n+2) { border-bottom: none !important }
    .pdp-spec-card__label { text-transform: none !important; letter-spacing: .01em !important; font-size: 13px !important }

    /* ── 2. Editorial: text-only, single column, generous width ── */
    .pdp-editorial { padding: 64px 0 56px !important }
    .pdp-editorial__copy { max-width: 680px }
    .pdp-editorial__lead { font-size: 19px !important; color: #4b5563 !important }
    .pdp-editorial__body { font-size: 17px !important; color: #5a5a5f !important }

    /* ── 3. Specs table: editorial feel, not spec-sheet ── */
    .pdp-details { padding: 40px 0 48px !important }
    .pdp-specs-table td { padding: 18px 0 !important; border-bottom: 1px solid rgba(0,0,0,.04) !important }
    .pdp-specs-table td:first-child {
      color: rgba(0,0,0,.38) !important;
      font-size: 12px !important;
      font-weight: 500 !important;
      text-transform: none !important;
      letter-spacing: .01em !important;
    }
    .pdp-specs-table td:last-child { font-size: 15px !important; font-weight: 500 !important; color: #111 !important }
    .pdp-accordion { border-top: 1px solid rgba(0,0,0,.06) !important }
    .pdp-accordion:last-child { border-bottom: 1px solid rgba(0,0,0,.06) !important }

    /* ── 4. Box item icons ── */
    .pdp-box-item__icon { display: block; margin: 0 auto 10px; color: rgba(0,0,0,.4) }
    .pdp-box-item__icon svg { display: block; margin: auto }
    .pdp-compare-card__specs { display: none !important }

    /* ── 5. Compare: current dominant, secondary muted ── */
    .pdp-compare { background: var(--pdp-bg) !important; padding: 72px 0 !important }
    .pdp-compare-card--current {
      background: #fafaf8 !important;
      border: 2px solid #111 !important;
      transform: none !important;
      box-shadow: 0 8px 32px rgba(0,0,0,.1) !important;
    }
    .pdp-compare-card--alt {
      background: var(--pdp-surface) !important;
      border: 1px solid rgba(0,0,0,.06) !important;
      box-shadow: none !important;
      opacity: .72;
      transition: opacity .2s ease, box-shadow .2s ease !important;
    }
    .pdp-compare-card--alt:hover { opacity: 1 !important; box-shadow: 0 4px 16px rgba(0,0,0,.07) !important }
    .pdp-compare-card--alt h3 { font-size: 15px !important; color: #555 !important }
    .pdp-compare-card--alt .pdp-compare-card__price { font-size: 16px !important; font-weight: 500 !important; color: #666 !important }
    .pdp-compare-card--alt .pdp-compare-card__cta { background: transparent !important; color: var(--pdp-muted) !important; border: 1px solid rgba(0,0,0,.12) !important; font-size: 13px !important }

    /* ── 6. Vertical rhythm ── */
    /* Hero: breathe more */
    .pdp-hero { padding: 100px 0 40px !important }
    /* Specs+box: tighter */
    .pdp-accordion__trigger { padding: 20px 0 !important }
    .pdp-accordion__body { padding: 0 0 20px !important }
  </style>
</Layout>
